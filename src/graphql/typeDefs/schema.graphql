type Query {
    users(input: UsersInput): Page
    user(userId: ID!): User
}

type Mutation {
    createUser(input: CreateUserInput!): User
    updateUser(input: UpdateUserInput!): Boolean
    addUserFriend(input: AddFriendInput!): Boolean
}
